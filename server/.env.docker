PORT=7000

DB_HOST=db
DB_NAME=online_store
DB_USER=postgres
DB_PASS=postgres
DB_PORT=5432

BCRYPT_SALT_ROUNDS=10
# SECRET_KEY and PAYMENT_WEBHOOK_SECRET are injected from root .env via docker-compose.

# CORS
CORS_ORIGINS=http://localhost:3000

# App security
RATE_LIMIT_MAX=300

# File upload (bytes)
UPLOAD_MAX_FILE_SIZE=5242880
